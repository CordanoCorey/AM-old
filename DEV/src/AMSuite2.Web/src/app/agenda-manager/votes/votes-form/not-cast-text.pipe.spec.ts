import { NotCastTextPipe } from './not-cast-text.pipe';

describe('NotCastTextPipe', () => {
  it('create an instance', () => {
    const pipe = new NotCastTextPipe();
    expect(pipe).toBeTruthy();
  });
});
